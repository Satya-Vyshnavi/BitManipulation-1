T.c --> O(n)
S.c --> O(1)

class Solution {
    public int singleNumber(int[] nums) {
        if(nums == null || nums.length == 0){
            return 0;
        }
        int xor = 0;
        for(int i =0; i<nums.length; i++){
            xor = xor ^ nums[i];
        }
        return xor;
    }
}